node_modules/
.claude/
test-results/
package-lock.json
contexta-recorder.zip

# Extension signing private key — must not be committed
.extension-signing-key.pem
contexta-recorder-v*.zip

# Internal docs — kept locally for reference but not published
docs/
